#!/bin/sh

psql << EOF

CREATE DATABASE $DB_NAME OWNER $POSTGRES_USER;
GRANT ALL PRIVILEGES ON DATABASE $DB_NAME TO $POSTGRES_USER;

EOF

#pg_restore -d $DB_NAME /docker-entrypoint-initdb.d/$POSTGRES_DB.dump
